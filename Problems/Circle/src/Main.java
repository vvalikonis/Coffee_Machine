class Circle {

    double radius;

    // write methods here
    double getLength() {
        return 2.0 * radius * Math.PI;
    }
    double getArea() {
        return Math.PI * radius * radius;
    }
}